import { AddDurationDatePipe } from './add-duration-date.pipe';

describe('AddDurationDatePipe', () => {
  it('create an instance', () => {
    const pipe = new AddDurationDatePipe();
    expect(pipe).toBeTruthy();
  });
});
